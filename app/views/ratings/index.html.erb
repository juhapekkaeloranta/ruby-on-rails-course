<h2>List of ratings</h2>

<%= link_to 'New rating', new_rating_path %>

<ul>
  <% @ratings.each do |rating| %>
    <li> <%= rating %> <%= link_to 'Destroy', rating_path(rating.id), method: :delete, data: { confirm: 'Are you sure?' } %> </li>
  <% end %>
</ul>